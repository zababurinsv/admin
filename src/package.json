{
  "name": "system",
  "version": "0.0.1",
  "description": "docs",
  "main": "index.js",
  "authors": {
    "name": "Sergey",
    "email": "s.zababurin.v@gmail.com"
  },
  "repository": "https://github.com/zababurinsv/admin.git",
  "scripts": {
    "build": "vuepress build src -d ../docs/",
    "remove": "del-cli --force  ../docs/",
    "deploy": "npm run build && cd .. && git status && git add -A && git commit -am 'update' && git push"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@silvanite/vuepress-plugin-markdown-classes": "^1.0.2",
    "@vuepress/plugin-back-to-top": "^1.8.2",
    "@vuepress/plugin-medium-zoom": "^1.8.2",
    "del-cli": "^3.0.1",
    "markdown-it-imsize": "^2.0.1",
    "vuepress": "^1.5.3"
  },
  "dependencies": {
    "vue-sse": "^2.0.2"
  }
}
